{% extends 'base.html.twig' %}

{% block title %}Modifier la figure{{ figure.title }}{% endblock %}

{% block body %}
    <h1>Modifier la figure "{{ figure.title }}"</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">Modifier</button>
    {{ form_end(form) }}
{% endblock %}
